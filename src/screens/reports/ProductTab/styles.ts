import styled from 'styled-components/native';
import { margin } from '../../../styles';

export const Item = styled.View`
  flex-direction: row-reverse;
  justify-content: space-between;
  align-items: center;
  padding: ${margin.thin}px ${margin.normal}px;
`;
